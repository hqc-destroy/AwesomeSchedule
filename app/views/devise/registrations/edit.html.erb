<% provide :title, "ユーザーの編集" %>

<div class="signup-form">
  <div class="container">
    <div class="row" style="padding-top: 5%;padding-bottom: 3%">
      <h2 style="margin: 0px auto; color: #00828C;font-weight: bold"> ユーザーの編集 </h2>
    </div>
    <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
      <%= render "devise/shared/error_messages", resource: resource %>
      <div class="form-group">
        <i class="fa fa-envelope-o" aria-hidden="true" style="font-size: 25px;font-weight: bold;color:white;padding-right: 10px"></i>
        <%= f.label "メールアドレス", style: "color:white;font-size:large;font-weight:bold" %><br />
        <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "form-control", require: true  %>
      </div>

      <div class="form-group">
        <i class="fa fa-user" aria-hidden="true" style="font-size: 25px;font-weight: bold;color:white;padding-right: 10px"></i>
        <%= f.label "名前", style: "color:white;font-size:large;font-weight:bold" %><br />
        <%= f.text_field :name, autocomplete: "name", class: "form-control" , require: true %>
      </div>

      <div class="form-group">
        <i class="fa fa-briefcase" aria-hidden="true" style="font-size: 25px;font-weight: bold;color:white;padding-right: 10px"></i>
        <%= f.label "仕事", style: "color:white;font-size:large;font-weight:bold" %><br />
        <%= f.text_field :job, autocomplete: "job", class: "form-control" , require: true %>
      </div>
      <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
        <div>Currently waiting confirmation for: <%= resource.unconfirmed_email %></div>
      <% end %>
      <div class="form-group">
        <%= f.label "パスワード", style: "color:white;font-size:large;font-weight:bold" %> <i style="color:black;font-size:medium;font-weight:bold">(変更したくない場合は空白のままにします)</i><br />
        <%= f.password_field :password, autocomplete: "new-password" %>
        <% if @minimum_password_length %>
          <br />
          <em style="font-size: medium;color: black;font-weight: bold;"><%= @minimum_password_length %> characters minimum</em>
        <% end %>
      </div>

     <div class="form-group">
        <%= f.label "パスワードの確認", style: "color:white;font-size:large;font-weight:bold" %><br />
        <%= f.password_field :password_confirmation, autocomplete: "new-password" %>
      </div>

      <div class="form-group">
        <%= f.label "現在のパスワード", style: "color:white;font-size:large;font-weight:bold" %> <i style="color:black;font-size:medium;font-weight:bold">(変更を確認するには、現在のパスワードが必要です)</i><br />
        <%= f.password_field :current_password, autocomplete: "current-password" %>
      </div>

      <div class="form-group">
        <%= f.label :avatar, style: "color:white;font-size:large;font-weight:bold" %> <i style="color:black;font-size:medium;font-weight:bold">(プロファイルをアップロードする)</i> <br/>
        <%= f.file_field :avatar %><br>
      </div>

      <div class="row">
        <button type="submit" class="btn btn-warning" style="margin:0px auto;font-size: large;color: black">更新</button>
      </div>
    <% end %>

  </div>
</div>
